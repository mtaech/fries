server:
  port: 8888
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: rt123456
    url: jdbc:postgresql://localhost:5432/fries

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update